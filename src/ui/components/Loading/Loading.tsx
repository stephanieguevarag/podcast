import React from "react";
import { Wrapper, Child, Container } from "./Loading.styles";

const Loading = () => {
  return (
    <Container>
      <Wrapper>
        <Child></Child>
        <Child></Child>
        <Child></Child>
        <Child></Child>
        <Child></Child>
        <Child></Child>
        <Child></Child>
        <Child></Child>
        <Child></Child>
      </Wrapper>
    </Container>
  );
};

export default Loading;

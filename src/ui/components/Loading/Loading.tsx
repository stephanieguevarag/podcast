import React from "react";
import { Wrapper, Child } from "./Loading.styles";

const Loading = () => {
  return (
    <Wrapper>
      <Child></Child>
      <Child></Child>
      <Child></Child>
      <Child></Child>
    </Wrapper>
  );
};

export default Loading;
